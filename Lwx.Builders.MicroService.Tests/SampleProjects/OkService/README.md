# OkService sample project

This project contains valid Lwx microservice sample that demonstrates typical usage of the generator:
- Service.cs with proper Service.Configure methods
- Endpoints under `Endpoints` namespace with [LwxEndpoint]
- Workers under `Workers` namespace with [LwxWorker]

Build and run with `dotnet build` and `dotnet run`.
